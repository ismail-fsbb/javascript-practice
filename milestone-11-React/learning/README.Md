# 📘 React Learning Journey – Milestone 1

Welcome to my React learning journey! This repository contains my practical implementation of **Milestone 1**, designed to help me (and others) understand the core concepts of React in Bangla (বাংলা ব্যাখ্যা সহ)।  

---

## 🎯 Milestone 1: Getting Started with React

### Topics Covered:

1. ✅ **What is React?**
   - React হলো একটি JavaScript লাইব্রেরি যা ইউজার ইন্টারফেস (UI) তৈরি করার জন্য ব্যবহৃত হয়।
   - Component-based structure, fast rendering using Virtual DOM, one-way data binding.

2. ✅ **JSX**
   - JavaScript XML — HTML এর মতো দেখতে syntax যা আমরা JavaScript এর ভিতরে লিখি।
   - JSX এর ভিতরে JavaScript expression লিখতে `{}` ব্যবহার করি।

3. ✅ **Components**
   - Functional Component তৈরি করা এবং তা re-use করা।
   - Component গুলো ছোট ছোট reusable unit হিসেবে কাজ করে।

4. ✅ **Props**
   - Component এ বাইরে থেকে ডেটা পাঠানোর জন্য ব্যবহার হয়।
   - Parent → Child data পাঠানো হয় props দিয়ে।

5. ✅ **State**
   - Component এর নিজস্ব ডেটা বা অবস্থা। State change হলে UI auto-update হয়।
   - State declare করার জন্য `useState` Hook ব্যবহার করা হয়।

6. ✅ **Event Handling**
   - ইউজারের action (যেমন: click, change) handle করার জন্য।
   - React এ `onClick`, `onChange` ইত্যাদি ইভেন্ট attribute ব্যবহার করা হয়।

7. ✅ **useEffect & useState**
   - `useState`: component এর state তৈরি ও আপডেট করার জন্য।
   - `useEffect`: side effects (যেমন API call, DOM manipulation) handle করার জন্য।

---
